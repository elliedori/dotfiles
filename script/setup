#!/usr/bin/env bash

DIR="$(cd "$( dirname "${BASH_SOURCE[0]}" )/../" && pwd)"
echo $DIR

ln -s "$DIR/bash_profile" "$HOME/.bash_profile"
ln -s "$DIR/git_config" "$HOME/.gitconfig"
ln -s "$DIR/input_rc" "$HOME/.inputrc"
ln -s "$DIR/tmux.conf" "$HOME/.tmux.conf"
